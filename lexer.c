#include "compiler.h"

// ch7: stub. The lexer doesn't read any input yet; ch8 introduces the
// first real token (number) and the read_next_token() dispatch.
int lex(struct lex_process* process){
    return LEXICAL_ANALYSIS_ALL_OK;
}
